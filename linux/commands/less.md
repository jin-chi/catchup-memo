# less
ファイルを参照するだけのコマンド。検索する機能もある。  
また、`gzip`形式のファイルであれば`less`コマンドが内部的に解凍してくれるので、別途解凍しなくてもそのまま`less`コマンドだけでファイルの中身を参照することができる。

```bash
# gzipファイルは解凍せずにlessできる。
less test.gz
```

## `-i` (`--ignore-case`) オプション
ファイルを開いた後の検索機能で、大文字小文字を区別しない検索をする設定で開くことができる。
```bash
less -i
```